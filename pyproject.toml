[project]
name = "pythonproject5"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytelegrambotapi>=4.35.0",
    "python-dotenv>=1.2.2",
]
